## Debug mode

В пакете поддерживается **режим отладки (debug mode)**, предназначенный для разработки и локального тестирования.

### Включение debug mode

Debug-режим включается через конфигурацию приложения / переменные окружения:

```env
APP_DEBUG=true
```
---

### Поведение API в debug mode

При включённом debug-режиме:

#### 📦 HTTP-ответы API
В ответы API **добавляется расширенная отладочная информация**, которая может включать:
- детали ошибки;
- технические сообщения;
- дополнительные поля, полезные при разработке.

> ⚠️ В production-окружении debug-режим **должен быть выключен**, так как отладочная информация может содержать чувствительные данные.

---

#### 📝 Логирование

В debug-режиме логирование расширяется и дополнительно включает:

- HTTP-заголовки запроса (с маскированием чувствительных данных);
- параметры запроса (query params);
- тело запроса (для JSON-запросов);

Это позволяет:
- быстрее диагностировать ошибки;
- анализировать входящие запросы;
- упрощать локальную отладку и тестирование.

---

### Безопасность

- Чувствительные заголовки (например `Authorization`, `Cookie`) **маскируются** и не выводятся в явном виде.
- В production-окружении рекомендуется использовать минимальный уровень логирования без debug-информации.

---

### Рекомендации

- ✅ Используйте debug-режим **только в dev / staging**
- ❌ Не включайте debug-режим в production
- 🔍 Для анализа ошибок в production используйте централизованные системы логирования и трейсинга (например, Sentry)
